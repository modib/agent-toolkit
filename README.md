# goose-config

Meta-repository tracking Goose AI agent configuration, skills, and goals.

## Contents
- `~/.agents/skills/` — Custom skills loaded by goose
- `~/.config/goose/` — Goose configuration files
- `~/.config/goose/goals/` — Active goal plans and progress

## Setup
This repo tracks goose's configuration for portability and backup.
